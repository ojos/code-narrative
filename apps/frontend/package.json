{
  "name": "@code-narrative/frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "code-narrative 管理画面 (CloudFront + S3 静的 SPA / Cognito PKCE)",
  "scripts": {
    "lint": "node scripts/check.js",
    "test": "node --test",
    "build": "node scripts/build.js",
    "verify": "npm run lint && npm run test && npm run build"
  },
  "engines": {
    "node": ">=18"
  }
}
